# Командная лабораторная работа №1 #
3 семестр
<<<<<<< HEAD
- Разработчики:
  1. Гаврилов Никита
  2. Власов Леонид
  3. Созинов Алексей

---
  # :star: Неделя 1
---
  
  # Постановка задачи #
- Разработать программную систему для алгебраических операций над полиномами от 3 переменных.
- Хранение мономов полинома: кольцевой список с фиктивным звеном
- Набор полиномов должен храниться в таблицах 6 видов:
    1. Линейная на массиве.
    2. Линейная на списке.
    3. Упорядоченная на массиве.
    4. На поисковом дереве.
    5. Хеш-таблица с открытым перемешиванием.
    6. Хеш-таблица со списками (метод цепочек).
- Ключ для поиска в таблице: имя полинома.
- Операции над полиномами: сложение, вычитание, умножение, деление(целочисленное и с остатком), дифференцирование, интегрирование, вычисление в точке.
- Операции выполняются в постфиксной форме.
 
# Сроки выполнения #
	Начало - 14 февраля 2020
	Окончание - 10 апреля 2020
    
# Требования #
- Программная система должна обеспечивать выполнение следующих функций:
    1. Принимать введенный полином.
    2. Генерировать уникальное имя полученному полиному (ключ).
    3. Обрабатывать его и сохранять в 6 разных таблицах.
    4. Искать полином по ключу.
    5. Удалять полином из всех таблиц.
    6. Выполнять математические операции с полиномами.
    7. Выводить результат на экран.

- Программная система разрабатывается в Microsoft Visual Studio 2019

# Разработка #
- Этапы разработки
    1. Составление списка объектов и основных алгоритмов.
    2. Описание модели объектов и основных алгоритмов.
    3. Подготовка проекта программной системы.
    4. Заготовки классов в h-файле.
    5. Заготовки форм.
    6. Разработка пустых (add failure) версий модульных тестов для методов классов таблиц (с использованием библиотеки google tests).
    7. Разработка тестов и кода.

---

# Распределение работ #
- Гаврилов Никита
  - Интерфейс. Линейная таблица на массиве. Таблица на поисковом дереве(АВЛ).
- Власов Леонид
  - Полином. Линейная таблица на списке. Хеш-таблица с открытым перемешиванием. Общий интерфейс таблиц.
- Созинов Алексей
  - Постфикс для полиномов. Упорядоченная таблица на массиве. Хеш-таблица со списками (метод цепочек).

  
---
  # :star: Неделя 2
---

# Список объектов #
  - Моном
  - Полином
  - Таблицы
    - Линейная на массиве
    - Линейная на списке
    - Упорядоченная на массива
    - Дерево(АВЛ)
    - Хеш таблица с открытым перемешиванием
    - Хеш таблица со списками
	
# Список основных алгоритмов #
  - Полином
    - сложение
    - вычитание
    - умножение
    - деление целочисленное
    - деление с остатком
    - дифференцирование
    - интегрирование
    - вычисление в точке
  - Таблицы
    - вставка
    - удаление
    - поиск
    - сортировка(для упорядоченных)
    - ввод/ввывод в файл
	
# Описание моделей объектов #
  - Моном - произведение, коэффициента и 3 переменных, в целой степени в пределе от 0 до 65536
  - Полином - сумма различных мономов, хранящихся в списке
  - Таблицы - различаются только способом хранения, а хранят один и тот же элемент - структура(ключ, полином, состояние(удален/не удален)), где ключ - строка(название полинома)
    - Линейная на массиве - неупорядоченный массив элементов
    - Линейная на списке - неупорядоченный двунаправленный список элементов, каждый элемент которого дополнительно содержит указатель на 1 следующий элемента и на 1 предыдущий
    - Упорядоченная на массива - упорядоченный массив элементов
    - Дерево(АВЛ) - сбалансированное по высоте двоичное дерево поиска, каждый элемент которого дополнительно содержит разницу высот поддеревьев, указатель на 2 следующих элемента(один указатель относиться к элементу который лексиграфичечки меньше этого, другой - к большему) и на 1 предыдущий
    - Хеш таблица с открытым перемешиванием TODO
    - Хеш таблица со списками TODO
	  
# Описание основных алгоритмов #
  - Полином
    - сложение, вычитание - каждый моном 2го полинома прибаляется к 1му полиному, если этот моном не содержится в 1ом полиноме, 
	иначе складываются коэффициенты этих мономов(при вычитании коэффициенты мономов 2го полинома инвертируются)
	- умножение - декартово произведение мономов 1го полинома на мономы 2го полинома
	- деление TODO
	- дифференцирование - взятие частной производной
	- интегрирование - взятие частного интеграла
	- вычисление в точке - переменным присваиваются значения, и полученное выражение вычисляется
  - Таблицы
    - Линейная на массиве
	  - вставка - производится поиск, в случае нахождения неудаленного бросается ошибка, 
	  иначе в конец массива добавляется новый элемент(в случае нахождения неудаленного элемент замещается), при отсутствии места массив переписывается с двойным размером
	  - удаление - производится поиск, в случае нахождения неудаленного присваивается состаяние "удален" иначе выводится ошибка
	  - поиск - прохождение по всему массиву, сравнение ключа, в случае нахождения неудаленного возвращается true и передается ссылка на полином через аргументы, 
	  иначе возвращается false
	  - запись в файл - каждый неудаленный полином записывается в файл в каждую отдельную строку
	  - чтение из файла - каждая строчка конвертируется в полином и этот полином вставляется в массив
	  
    - Линейная на списке
	  - вставка - производится поиск, в случае нахождения неудаленного бросается ошибка, 
	  иначе в конец списка добавляется новый элемент(в случае нахождения неудаленного элемент замещается)
	  - удаление - производится поиск, в случае нахождения неудаленного присваивается состаяние "удален" иначе выводится ошибка
	  - поиск - прохождение по всему списку, сравнение ключа, в случае нахождения неудаленного возвращается true и передается ссылка на полином через аргументы, 
	  иначе возвращается false
	  - запись в файл - каждый неудаленный полином записывается в файл в каждую отдельную строку
	  - чтение из файла - каждая строчка конвертируется в полином и этот полином вставляется в список
	  
    - Упорядоченная на массива
	  - вставка - производится поиск, в случае нахождения неудаленного бросается ошибка, 
	  иначе в массив добавляется новый элемент в подходящее место по порядку(в случае нахождения неудаленного элемент замещается), 
	  при отсутствии места массив переписывается с двойным размером
	  - удаление - производится поиск, в случае нахождения неудаленного присваивается состаяние "удален" иначе выводится ошибка
	  - поиск - производится бинарный поиск, в случае нахождения неудаленного возвращается true и передается ссылка на полином через аргументы, иначе возвращается false
	  - запись в файл - каждый неудаленный полином записывается в файл в каждую отдельную строку
	  - чтение из файла - каждая строчка конвертируется в полином и этот полином вставляется в массив
	  - сортировка - быстрая сортировка по ключу полинома
		
    - Дерево(АВЛ)
	  - вставка - производится поиск, в случае нахождения неудаленного бросается ошибка, 
	  иначе сравниваем с последним элементом в поиске и вставляем по лексиграфическому порядку в один из указателей, затем пересчитываются разницы высот для всех родительских вершин, после производится балансировка дерева
	  - удаление - производится поиск, в случае нахождения неудаленного присваивается состаяние "удален" иначе выводится ошибка
	  - поиск - производится сравнение с корневым элементом, если меньше то идем по указателю на меньший элемент, если больше - на больший элемент. В случае равенства при условии неудаленности возвращается true и передается ссылка на полином через аргументы. В случае ненахождения возвращается false
	  - запись в файл - каждый неудаленный полином записывается в файл в каждую отдельную строку
	  - чтение из файла - каждая строчка конвертируется в полином и этот полином вставляется в массив
	  - балансировка - https://habr.com/ru/post/150732/ (Балансировка узлов)
	  
    - Хеш таблица с открытым перемешиванием
    - Хеш таблица со списками

